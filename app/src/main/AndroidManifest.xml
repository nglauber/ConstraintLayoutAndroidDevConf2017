<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nglauber.constraintlayouttest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PercentActivity" />
        <activity android:name=".PlaceholderActivity" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".BarriersActivity" />
        <activity android:name=".ConstraintSetActivity" />
        <activity android:name=".BiasActivity" />
        <activity android:name=".GoneMarginActivity" />
        <activity android:name=".ChainsActivity" />
        <activity android:name=".RatioActivity" />
        <activity android:name=".NetflixActivity"></activity>
    </application>

</manifest>